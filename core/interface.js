// --------------------------------------------------------
// Main esdoc object exposes various utility functions.
// --------------------------------------------------------
(function (root) {
    // ECMAScript 5 Strict Mode
    "use strict";

    // Initialise root object.
    root.CIM = root.cim = root.ESDOC = root.esdoc = {
        // App version.
        VERSION: '0.12.0.0',

        // App name.
        NAME: "Earth System Documentation",

        // Utilities namespace.
        utils: {
            views : {}
        }
    };

}(this));
