// --------------------------------------------------------
// utils.constants.js - constants used across application.
// --------------------------------------------------------
(function(ESDOC, _) {
    // ECMAScript 5 Strict Mode
    "use strict";

    // Declare constants used within search.
    _.extend(ESDOC.search, { constants : {
        // Value to display whenever a field is null.
        NULL_FIELD : '--'
    }});
    
}(this.ESDOC, this._));
